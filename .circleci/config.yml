version: 2.1

jobs:

  toxify:
      docker:
        - image: fkrull/multi-python

      parameters:
        tox_env:
          type: string

      steps:
        - checkout
        - run: pip install tox
        - run: tox -e << parameters.tox_env >>




workflows:
  version: 2

  myproj:
    jobs:
      - toxify:
          tox_env: 'style'

